# HCOM Phase 2 阶段报告

> 报告日期：2026-09-12
> 阶段：Phase 2 — Windows UART 工作台与交互完善
> 当前版本：`0.2.0`
> 状态：已完成本地实现、自动化验证与 Windows Release 打包；真实设备的长期连续收发仍需实机验收。

## 结论

Phase 2 已将 Phase 1 的演示串口工作台升级为真实 Windows COM 通信工作台，并完成收发数据显示、分包策略、日志、发送队列、周期发送、响应式布局和 Windows Material 风格交互。Flutter 继续通过 UTF-8 NDJSON 调用 Rust Core；Core 负责真实串口枚举、打开、读取、写入与周期调度，工作台只依据实际 RX/TX 事件更新。

## 已完成内容

### 1. Rust UART Core 与协议边界

- 使用 `serialport` 进行 Windows COM 枚举、配置映射、打开/关闭、异步读取与真实写入。
- `scan_ports` 返回真实端口名、描述、硬件 ID 和端口类型；`open_port` 支持波特率、5–8 数据位、1/2 停止位、无/奇/偶校验与无/RTS-CTS/XON-XOFF 流控。
- RX 采用 6 ms 空闲时间作为传输批次边界，单批最多 4 KiB；Core→UI 队列固定为 128 个事件，慢速 UI 不会阻塞串口读取。
- 单次 HEX 写入上限为 16 KiB。只有实际写入成功后，Core 才产生 TX 事件。
- `start_periodic` / `stop_periodic` 在 Core 内调度发送；停止命令会直接清除调度任务，不再等待 Flutter 端定时器的下一轮回调。
- `protocol/stdio-ndjson.md` 已记录周期命令、周期状态事件，以及“Core 只给出传输边界、Flutter 才执行显示分帧”的职责边界。

### 2. 接收流、分包与日志

- 接收流按时间从上到下递增，最新 RX/TX 追加在底部；接收区支持文本拖选和复制。
- 实现通用 Flutter `ReceiveFramer`：自动识别稳定主导长度、按空闲间隔、固定字节数、帧头＋帧尾四种策略。接收工具栏以直接下拉菜单切换策略，不再弹出分包对话框；协议不确定时不会硬编码 `AA 55` 等特定帧头。
- 自动识别在连续稳定样本后，可将整倍数传输批次按学习到的长度拆分；固定长度和帧头帧尾模式可跨越任意系统读取边界。
- RX/TX 标识会随各自的 HEX／文本显示格式显示为 `RX(HEX)`、`TX(TEXT)` 等；字节计数不参与复制，避免复制结果混淆。
- 选中多条记录时，复制文本以“时间＋方向”行和“数据”行分隔，避免多行日志粘成一行。
- 接收工具栏已提供显示行号、清除日志、手动保存和实时保存。实时保存菜单明确提供关闭 / CSV / TXT，编译默认关闭；用户可在设置中选择启动时续写上次主动选择的保存文件。手动保存与实时保存可并行使用；默认日志时间为 UTC+08:00，用户可在设置中修改。

### 3. 发送、队列与组包

- 发送格式与接收显示格式可独立选择 HEX／普通文本；发送格式在 1.5 秒内连续切换三次后开启长期联动，之后每次发送格式切换立即同步接收格式；直接切换接收格式会解除联动。
- 普通直接发送、顺序、循环、触发和周期发送均有明确入口。周期发送仅针对当前发送框消息，不唤起队列编辑。
- 队列编辑位于右侧工作区，每项可勾选、命名、上移下移、设置发送后延时和组包后缀；删除使用 450 ms 二次点击快速确认，超时后改为确认对话框以避免误触。
- 发送框与队列每项均支持不追加、`\\r`、`\\r\\n`、`\\n` 组包后缀。

### 4. 桌面工作台与响应式体验

- 接入自定义 Material 3 风格 Windows 标题栏，并保留系统最小化、最大化和关闭操作。
- 中央接收区优先保留空间：宽度小于 600 px 时禁用队列编辑展开并给出通知；半屏宽度时接收区和队列编辑各占约一半。
- 左右 Dock 胶囊和外侧展开／收起箭头都固定在同一条窗口竖向中心线上，悬停显示、离开半透明；短窗口会自适应压缩 Dock 项。
- 串口配置、发送面板、队列和主题均可设置启动默认值。串口配置收起时保留连接摘要及可恢复箭头。
- 全部可操作按钮提供悬浮提示；通知在鼠标悬停时淡出并放行下方控件。
- 统一下拉与菜单为圆角 Material 菜单：菜单面板 16 px 圆角，悬停／选中项 10 px 圆角，覆盖接收分包、实时保存、手动保存与组包选项。
- 中文使用 Noto Sans SC，英文与数值使用随包分发的 Cascadia Code。

### 5. 打包与依赖

- Windows CMake 在打包时构建并部署 `hcom-core.exe` 至 `hcom.exe` 同目录，避免 Release 包携带过期 Core。
- 引入 `bitsdojo_window` 提供自定义 Windows 窗口框架，`file_selector` 提供日志保存位置选择。
- Release 使用 `--no-tree-shake-icons`，保留 Material 图标字体，避免图标被增量缓存裁剪而缺失。

## 验证记录

- `cargo fmt --check`：通过。
- `cargo test --manifest-path core/Cargo.toml`：5/5 通过；覆盖 HEX 输入、串口参数映射和周期命令批次解析。
- `flutter analyze --no-pub`：通过，无问题。
- `flutter test --no-pub test/widget_test.dart`：20/20 通过；涵盖 Dock 中心对齐、窄窗口队列策略、格式联动、队列删除与移动、日志导出、分包策略及圆角菜单状态。
- `flutter build windows --release --no-tree-shake-icons`：通过。
- Release 包完整性：`hcom.exe`、`hcom-core.exe` 与 `data/app.so` 均存在于标准 Release 目录。
- Release 启动：`hcom.exe` 已成功启动并处于 Responding 状态，窗口标题为“HCOM 调试助手”。

## 明确边界与后续验收

- Core 的 6 ms 空闲边界是传输批次，不等同于任何设备协议的帧边界。固定长度、帧头＋帧尾等规则必须由用户按实际协议配置。
- 自动识别仅在稳定样本长度足够时拆分；对于长度可变、无明确边界的协议，不应把推断结果当作正式协议解析。
- 当前没有在本次改动后使用真实 USB-UART/虚拟 COM 对进行双向写入、拔插恢复和 8 小时连续运行验证；自动化测试与 Release 启动不能替代这些实机验收。
- 不包含 CRC/校验算法、字段解析、波形分析、TCP/蓝牙通信、插件系统或完整协议模板库。

## 建议的实机验收

1. 连接已知 USB-UART 或虚拟 COM 对，核对端口描述、硬件 ID 与 115200 8-N-1 打开/关闭。
2. 分别以固定长度与帧头＋帧尾模式双向发送多包数据，验证粘包、拆包和不规则读取边界。
3. 开始周期发送后立即停止，确认 Core 不再追加下一周期数据。
4. 分别保存 TXT 与 CSV，并核对时区、行号、字节计数和多条复制格式。
5. 拔出设备并连续运行目标波特率，记录断线恢复、`backpressure` 和丢包表现，再确定长期运行阈值。
