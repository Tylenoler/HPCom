# HCOM — 项目交接说明（Handoff）

> 用途：**给接手的 AI / 开发者使用**。读完这一份即可零上下文接手本项目，不需要回溯历史会话。
> 最后更新：2026-09-09
> 仓库：https://github.com/Tylenoler/HCOM

---

## 1. 项目是什么（30 秒看懂）

**HCOM** 是一款 **桌面端串口调试工具**，核心能力是 **双向 UART 收发 + 用户自定义协议帧解析**。

- 目标用户：嵌入式 / 硬件工程师
- 归属：按通用工具设计 + **自用与开源同步**，从作者自己的真实需求起步
- 形态：单一主程序（Flutter 桌面应用）+ 插件化能力扩展

> ⚠️ **历史提醒**：本项目曾在早期有另一个"旁路监听优先"的定义（旧 `项目管理文档.md`），**该定义已作废**。当前定义以本文件 + `产品说明书.md` 为准。作者已在 Phase 4 明确把“真实 COM + 虚拟 COM 桥接”的旁路监听纳入范围；它不是对任意被独占 COM 的共享读取。

---

## 2. 文档地图（先读哪些）

| 文件 | 内容 | 优先级 |
|------|------|--------|
| `交接说明.md` | **本文件** —— 项目全貌与接手须知 | ⭐ 第一份 |
| `产品说明书.md` | 产品完整定义（功能 / 架构 / 交互 / 里程碑 / 边界） | ⭐ 核心 |
| `设计规范.md` | 设计系统（M3 token / 字体 / 组件 / 布局 / Flutter 对照） | ⭐ 开发必读 |
| `sketches/002-material3/index.html` | **已采纳的 UI 原型**（可交互，浏览器直接打开） | ⭐ 视觉基准 |
| `sketches/001-material-dark/index.html` | 早期未采纳版本（仅存档参考） | 🗄️ 可选 |

Obsidian 侧同源文档：`Obsidian/Harper/Projects/嵌入式与硬件/HCOM-Debug-Assistant/项目说明书.md`

---

## 3. 已冻结的技术决策（不要再改，除非有充分理由）

| 维度 | 决策 | 状态 |
|------|------|------|
| **UI 前端** | **Flutter**（Google 开源套件，Material 3） | 🔒 已冻结 |
| **后端** | **Rust**（串口 + 协议分析，性能优先） | 🔒 已冻结 |
| **插件体系** | **双轨**：普通插件 Python / 高性能插件 Rust | 🔒 已冻结 |
| **插件接口** | 初版就定标准接口，考虑外部贡献者易用性（开源） | 🔒 已冻结 |
| **目标平台** | **Windows 桌面端为主** | 🔒 已冻结 |
| **通信类型（v1）** | 仅 UART / COM 串口（其他类型走插件扩展） | 🔒 已冻结 |
| **设计基准** | **Google Material 3（Material You）** | 🔒 已冻结 |
| **第一优先级** | UART 收发 + 协议帧解析 | 🔒 已冻结 |
| **非目标** | 不做示波器/逻辑分析仪/高速波形 | 🔒 已冻结 |

### 两条贯穿全程的底线
1. **「写速快」** —— 决定能否自用与开源同步推进
2. **「不能一卡一卡」** —— 调试工具，性能优先，绝不能卡顿

---

## 4. 核心功能定义（MVP 范围）

1. **双向 UART 收发** —— 主动发 + 接收解析，第一版仅 COM 口
2. **协议帧解析（核心中的核心）**：
   - 用户**可视化自定义帧格式**（拖库 + 点加）
   - 字段支持：帧头 / 帧尾 / 长度 / 数据域 / 校验（CRC、和校验）/ 载荷
   - 字段属性：名称 / 字节数 / 类型 / 字节序（大小端）/ 默认值
   - 校验可视化配置：CRC 类型 / 多项式 / 初值 / 异或
   - **实时预览**：用示例字节流当场验证解析是否正确
   - 帧模板保存 / 导入导出（JSON）/ 多帧 TAB 管理
3. **数据流分层展示**：接收区切换 HEX / 文本原始，字段解析由右侧 Dock 打开；两处使用同一数据流。
4. **发送面板**：指令队列（顺序 / 循环 / 触发）+ 定时发送 + 帧模板绑定 + 预设
5. **端口配置区**：COM 口扫描 / 波特率 / 数据位 / 停止位 / 校验 / 流控 + 连接状态机

> 详细定义见 `产品说明书.md` 第 3 节。

---

## 5. 当前进度

| 阶段 | 内容 | 状态 |
|------|------|------|
| **Phase 0** | 需求与边界冻结、产品说明书建立 | ✅ **已完成** |
| **Phase 1** | 框架基座 + 设计系统（Flutter 壳 + Rust 后端 + M3 主题） | ✅ **已完成** |
| **Phase 2** | Windows UART 通信内核与工作台交互 | ✅ **已完成** |
| **Phase 3** | 协议帧定义、校验与字段解析 | ✅ **已完成** |
| **Phase 4** | 发送面板深化与旁路监听桥接 | 🟡 **旁路监听与 Core 定时发送已完成；字段填充 / 条件发送待续** |

**已完成的具体产出：**
- ✅ 产品需求全部谈定并写成文档（功能 / 交互 / 布局 / 边界 / 里程碑）
- ✅ UI 设计基准确定（Material 3）+ 可交互原型 + 完整设计规范
- ✅ 仓库已初始化并推送（当前分支 `main`）

**当前可用能力：**
- ✅ Windows COM 枚举、打开/关闭、实际 RX/TX、周期发送与日志。
- ✅ 可视化帧定义器、字段拖拽排序、SUM-8 / CRC-16-MODBUS 校验、JSON 导入导出与字段解析视图。
- ✅ 真实 COM + 虚拟 COM 的旁路桥接、双向统一日志和 Rust Core 独立周期发送线程。
- 📌 详见 `阶段报告-Phase1.md`、`阶段报告-Phase2.md`、`阶段报告-Phase3.md`。

---

## 6. 下一步建议（Phase 4）

1. 完善发送面板的字段填充与模板绑定。
2. 在 Core 中接入条件触发前，先定义触发规则的稳定 NDJSON 契约。
3. 用真实 USB-UART + com0com 虚拟 COM 对验证旁路桥接、模板化协议跨批次接收与长时间稳定性。

> ⚠️ 前后端已确定为子进程 stdio UTF-8 NDJSON；修改 IPC 前必须先更新 `protocol/stdio-ndjson.md`。

---

## 7. 环境与仓库信息

| 项目 | 值 |
|------|-----|
| 仓库 | `https://github.com/Tylenoler/HCOM`（GitHub 账号：Tylenoler） |
| 本地路径 | `C:\Users\HARPER\OneDrive - Kylian\A_ITEM\Personal_Item\HCOM` |
| 默认分支 | `main` |
| 提交身份 | `Tylenoler <harperjeffrey0103@gmail.com>`（保持提交带头像） |
| 代理注意 | 本机 push GitHub 需走代理：`git config http.https://github.com.proxy http://127.0.0.1:7890`（已配置） |
| 原型查看 | 浏览器直接打开 `sketches/*/index.html`，无需构建 |

---

## 8. 接手须知（重要约定）

1. **不要重新推翻已冻结的决策**（见第 3 节）。这些是经过完整需求会谈确定的，改动前需与作者确认。
2. **设计以 `设计规范.md` + 原型为准**，新界面沿用同一套 M3 token，不要另起风格。
3. **第一优先级永远是 UART 收发 + 协议帧解析**；旁路监听仅限已实现的真实 COM + 虚拟 COM 桥接，不扩展为任意端口共享截获。
4. **性能是硬指标**：数据流处理不能卡顿（这是调试工具的生命线）。
5. **文档要持续维护**：功能变更同步更新 `产品说明书.md` 与 `设计规范.md`，并记录演进。
6. 本项目管理规范遵循作者的 **project-lifecycle** 约定（项目说明书 / 项目日志 / 项目结果等文档）。

---

## 9. 交接历史

| 日期 | 交接内容 |
|------|---------|
| 2026-09-09 | 项目重启：清空旧仓库内容与历史，重新定义产品；完成需求会谈、产品说明书、设计规范与 UI 原型；本文档建立，准备交接 |
| 2026-09-12 | 完成 Phase 1–3：Windows UART 工作台、协议帧定义与字段解析；详见各阶段报告。 |
| 2026-09-13 | 作者将旁路截获纳入 Phase 4；实现真实 COM + 虚拟 COM 桥接、Core 独立周期线程和 IPC v2，实机验收待完成。 |
