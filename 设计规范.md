# HPCom — 设计规范（Design System）

> 版本：v1.0（2026-09-09）
> 设计基准：**Google Material 3 (Material You)**
> 对应原型：`sketches/002-material3/index.html`（已确认采纳）

---

## 0. 一句话

HPCom 的界面**严格采用 Google Material 3（Material You）设计语言**：Roboto 字体 + Material Symbols 图标 + M3 tonal 配色 + M3 原版组件。

**为什么选 M3**：技术栈已定为 **Flutter**（Google 框架），其内置 `Material` 组件库天生就是 M3 —— 原型中的按钮 / 导航栏 / Tabs / Segmented Button 在 Flutter 中可**几乎 1:1 直接落地**，开发成本最低且规范耐看。

---

## 1. 设计基准（Design Baseline）

| 项目 | 规范 |
|------|------|
| 设计语言 | **Material 3 (Material You)**，Google 官方 |
| 界面字体 | **Roboto**（400 / 500 / 700） |
| 数据字体 | **Roboto Mono**（400 / 500）—— 用于 HEX / 时间戳 / 端口参数 |
| 图标 | **Material Symbols Rounded**（Google 官方图标字体，非 emoji） |
| 应用 Logo | `Image/LOGO.png`；用于顶栏品牌标识，以及 Windows EXE / 窗口 / 任务栏图标 |
| 主题 | **深色为默认**，支持一键切换浅色（完整 M3 light scheme） |
| 界面语言 | 中英双语，中文为主 |

---

## 2. 色彩 Token（Color Scheme）

### 2.1 深色主题（默认 / Dark）

```css
--primary:            #a8c7fa   /* 主色 */
--on-primary:         #0a305f
--primary-container:  #284777
--on-primary-container:#d6e3ff
--secondary-container:#3b4758
--on-secondary-container:#d7e3f8

--surface:            #101418   /* 应用背景 */
--sc-lowest:          #0b0f13   /* 数据流列表底 */
--sc-low:             #181c20   /* 卡片 / 导航栏 */
--sc:                 #1c2024   /* 顶栏 */
--sc-high:            #262a2f
--sc-highest:         #31353a

--on-surface:         #e2e2e9   /* 主文字 */
--on-surface-variant: #c4c6cf   /* 次要文字 */
--outline:            #8e9099
--outline-variant:    #44474e

--error:              #ffb4ab   /* 关闭端口等危险操作 */
--error-container:    #93000a
--ok:                 #7bd88f
--rx:                 #a8c7fa   /* 接收方向色 */
--tx:                 #7bd88f   /* 发送方向色 */
```

### 2.2 浅色主题（Light）

```css
--primary:            #0b57d0
--on-primary:         #ffffff
--primary-container:  #d3e3fd
--on-primary-container:#041e49
--secondary-container:#dee3eb
--on-secondary-container:#191c20

--surface:            #f8fafd
--sc-lowest:          #ffffff
--sc-low:             #f3f6fc
--sc:                 #edf1f7
--sc-high:            #e7ecf2
--sc-highest:         #e1e5ec

--on-surface:         #1a1c1e
--on-surface-variant: #43474e
--outline:            #73777f
--outline-variant:    #c3c6cf

--error:              #b3261e
--ok:                 #146c2e
--rx:                 #0b57d0
--tx:                 #146c2e
```

### 2.3 语义色约定（全应用统一）

| 语义 | 用途 | 深色 | 浅色 |
|------|------|------|------|
| **RX（接收）** | 数据流接收方向标识、字段高亮 | `#a8c7fa` 蓝 | `#0b57d0` 蓝 |
| **TX（发送）** | 数据流发送方向标识、字段高亮 | `#7bd88f` 绿 | `#146c2e` 绿 |
| **Error（危险）** | 关闭端口 / 校验失败 | `#ffb4ab` | `#b3261e` |
| **OK（成功）** | 连接成功 / 校验通过 | `#7bd88f` | `#146c2e` |

---

## 3. 排版（Typography）

| 用途 | 字体 | 字号 / 行高 | 字重 |
|------|------|------------|------|
| 应用标题（Top App Bar） | Roboto | 22px / 28 | 400 |
| 区块标题 | Roboto | 16px / 24 | 500 |
| 按钮 / TAB | Roboto | 14px / 20 | 500 |
| 正文 / 正文标签 | Roboto | 14px / 20 | 400 |
| 辅助标签（字段名 / tag） | Roboto | 11–12px | 400/500 |
| **数据流 / HEX / 端口参数** | **Roboto Mono** | 12.5–13px | 400/500 |

---

## 4. 组件规范（M3 Components）

| 组件 | M3 类型 | 用途 | 关键样式 |
|------|---------|------|---------|
| **Navigation Rail** | M3 Rail | 左侧协议切换 / 右侧字段解析与扩展面板 | 占位宽 88px；内层为悬浮全圆角 M3 Surface（上下 16px 留白、elevation 1）；选中项直接使用覆盖图标与文字的单一全圆 `secondary-container` 胶囊 |
| **Filled Button** | 主按钮 | 「发送」等主操作 | 圆角 100px，高 40px，`primary` 底 |
| **Tonal Button** | 次按钮 | 「+队列」等次操作 | 圆角 100px，`secondary-container` 底 |
| **Error Button** | 危险操作 | 「关闭端口」 | 圆角 100px，`error` 底 |
| **Segmented Button** | 分段按钮 | 发送模式：顺序 / 循环 / 触发 | 整体圆角 100px，选中项 `secondary-container` |
| **Segmented Button** | 分段按钮 | 接收区 HEX 原始 / 文本原始显示切换 | 位于接收区标题栏，与显示格式联动；字段解析不占用主视图区 |
| **Outlined Text Field** | 输入框 | 端口参数 / 发送输入 | 圆角 4px，浮动标签，聚焦时 2px `primary` 边框 |
| **Dropdown Menu** | 下拉菜单 | 串口与通信参数选项 | 圆角 16px、菜单零内边距；选中底色整行覆盖并由圆角裁切，选项需显示设备描述 |
| **Snackbar** | 提示条 | 操作反馈 | 底部浮出，圆角 8px，`sc-highest` 底 |
| **Split Button** | MD3 分裂按钮 | 顶栏串口摘要 + 串口配置展开/收起 | 左段显示连接状态与 `COM·波特率·参数`；右段为独立箭头操作区，以 1px 分隔线分段；整体 40px 高、20px 全圆角 |
| **Badge** | 计数徽标 | TAB 上的帧计数 | 圆角 8px，`primary-container` 底 |

### 圆角规范（Radius）
| 元素 | 圆角 |
|------|------|
| 按钮 / 胶囊 | 100px（full） |
| 卡片 / 面板 / 数据流容器 | 16px |
| 数据流列表行 | 12px |
| 输入框 | 4px |
| Chip / Badge / Tag | 8px |
| 对话框 | 28px |

---

## 5. 布局骨架（Layout Skeleton）

```
┌───────────────────────────────────────────────────────────────┐
│  Top App Bar  64px                                             │
│  [Logo+标题] [打开 保存 导入 导出 清除] ──── [连接状态Chip] [主题] [设置] │
├──────┬─────────────────────────────────────────────┬──────────┤
│ 左   │  上层：端口配置区（串口/波特率/数据位/停止位   │  右      │
│ Rail │         /校验/流控 + [关闭端口]）             │  Rail    │
│ 88px │  ─────────────────────────────────────────  │  88px    │
│      │  中层：接收区（HEX / 文本原始切换）            │ 字段解析 │
│ UART │  ─────────────────────────────────────────  │  CRC     │
│ CAN  │  下层：发送面板（模式 顺序/循环/触发            │  帧监听   │
│ CAN- │         + 指令队列 + 输入行）                 │  时序     │
│ FD   │                                             │  信号     │
│ I2C  │                                             │  抓包     │
│ TCP  │                                             │  插件     │
│ 虚拟  │                                             │          │
│ 设置  │                                             │          │
├──────┴─────────────────────────────────────────────┴──────────┤
│ Status Bar 30px   RX 50B · TX 6B · 速率 12.4KB/s   帧125 · 校验100% │
└───────────────────────────────────────────────────────────────┘
```

- 三区主干（上：端口配置 / 中：数据流 / 下：发送面板）+ 左右 Navigation Rail
- 左右 Rail 用于**协议切换**与**扩展插件入口**，与插件化架构呼应

---

## 6. 交互规范（Interaction）

| 交互 | 规范 |
|------|------|
| 主题切换 | 顶栏图标一键切深/浅色，全应用 token 联动 |
| 端口开关 | 「关闭端口」→ Error 色；「打开端口」→ Filled 色；底部 Snackbar 反馈；旁路监听使用同一连接区，显示虚拟 COM 对与“停止监听”状态 |
| Rail 选中 | 胶囊高亮移动，带 hover 态的 tonal 叠加 |
| TAB 切换 | 底部指示条滑动，选中项 primary 色 |
| 数据流 | RX 蓝 / TX 绿方向标；帧按块高亮（帧头/校验字段着色）；实时追加 |
| 发送 | 点击发送 → 数据流插入一条 TX + 状态栏 TX 计数跳动 + Snackbar |
| 状态反馈 | 所有操作给 Snackbar / 状态栏计数反馈 |

---

## 7. 动效规范（Motion）

动效只用于明确状态变化与空间关系，保持短促、一次性、可中断；**实时串口日志不为每条数据做入场动画**，避免高频数据下产生卡顿感。

| 场景 | 时长 | 曲线 / 实现 |
|------|------|-------------|
| Rail 选中胶囊 | 250ms | M3 强调式抵达。Flutter 使用 `Easing.emphasizedDecelerate`（官方将强调曲线拆分为 accelerate / decelerate） |
| 接收格式切换 | 200ms | `Easing.standard` |
| 深浅主题切换 | 200ms | MaterialApp 主题交叉渐变，`Easing.standard` |
| 连接状态 Chip | 180ms | 颜色过渡；连接成功时单次缩放脉冲，随后静止 |
| 可展开面板 | 200ms | `AnimatedSize` + `FadeTransition`，`Easing.standard` |
| Snackbar | 原生 | `ScaffoldMessenger` 的 Material 3 浮出反馈 |

> 不采用常驻粒子、循环装饰或为每条 RX/TX 日志添加动画。所有本节动效都应在状态稳定后停止，不保留计时器或后台刷新。

## 8. 原型文件位置

| 文件 | 说明 | 状态 |
|------|------|------|
| `sketches/002-material3/index.html` | **Material 3 原型（采纳）** | ✅ 设计基准 |
| `sketches/001-material-dark/index.html` | 早期自绘深色版（未采纳，可参考） | 🗄️ 归档 |

> 查看方式：浏览器直接打开对应 `index.html`（无需构建、无依赖）。

---

## 9. 落到 Flutter 的对照表（给开发）

| 原型元素 | Flutter 对应 |
|---------|-------------|
| Top App Bar | `AppBar` |
| Navigation Rail | `NavigationRail` |
| Filled / Tonal / Outlined Button | `FilledButton` / `FilledButton.tonal` / `OutlinedButton` |
| Segmented Button | `SegmentedButton` |
| 接收格式切换 | `SegmentedButton` |
| Outlined Text Field | `TextField` + `InputDecoration(border: OutlineInputBorder())` |
| Snackbar | `ScaffoldMessenger.showSnackBar` |
| 深/浅主题 | `ThemeData(brightness:)` + `ColorScheme.fromSeed` 或自定义 `ColorScheme` |
| 主题 token | `Theme.of(context).colorScheme.*` |
| 动效 token | `Easing.*` + `AnimatedPositioned` / `AnimatedSize` / `AnimatedSwitcher` |

---

## 10. 演进记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.2 | 2026-09-13 | 旁路监听复用连接配置区、Material 3 状态与错误反馈，不另设异构界面。 |
| v1.0 | 2026-09-09 | 确立 Material 3 为设计基准，基于 `sketches/002-material3` 抽取完整设计规范 |
| v1.1 | 2026-09-10 | 确立 Rail、Tabs、主题、连接状态、面板与 Snackbar 的 Motion token |
