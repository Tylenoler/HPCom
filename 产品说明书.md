# HCOM — 产品说明书

> 版本：v1.0（2026-09-09，会谈定稿）
> 状态：🟢 产品定义已冻结
> 作者：Harper（产品/需求）· 卡莉（洽谈整理）

---

## 1. 一句话描述

一个以 **UART 串口通信 + 用户自定义协议帧解析** 为核心的桌面端工程调试工具。插件化架构，性能优先，UI 采用 Flutter（Material 风格）主打观赏性，面向嵌入式工程师，**自用 + 开源同步**。

---

## 2. 产品定位与归属

| 维度 | 结论 |
|------|------|
| 核心定位 | 以 UART 通信与协议帧解析为核心的调试工具 |
| 目标用户 | 广义嵌入式 / 硬件工程师 |
| 归属 | 按通用架构设计 + **自用 / 开源同步**，从自己需求起步 |
| 开发节奏 | 自己先做出来自己用，**坚持真实需求驱动** |

---

## 3. 核心功能（MVP）

### 3.1 通信能力
- **双向 UART 收发**：既能主动发送，也能接收解析
- **第一版只支持 UART/COM 串口**一种；其他通信类型（TCP、蓝牙等）后续通过插件扩展

### 3.2 协议帧解析（核心中的核心）
- **用户可视化自定义帧格式**：图形界面上拖拽定义帧结构
- 支持的帧字段：**帧头 / 帧尾 / 长度字段 / 字节序（大小端）/ 校验（CRC、和校验）**
- 解析结果**分层展示**：
  - Hex 原始字节流
  - 按字段拆分的高级结构化视图
  - 时间戳 + 方向（RX/TX）标注

#### 3.2.1 帧定义配置界面（交互形态）
- **操作范式**：拖库 + 点加两者都要——左侧"帧结构构件库"可拖到右侧画布，也可点"+"逐个添加
- **可拖拽构件（初版）**：帧头 / 长度 / 数据域 / 校验（CRC、和校验）/ 帧尾 / 载荷
- **字段属性编辑**：点击字段弹出属性编辑（名称 / 字节数 / 类型 / 字节序 / 默认值等）
- **实时预览**：拖好帧后，用示例字节流实时看到每个字段的解析结果，验证帧定义是否正确
- **校验配置**：可视化下拉选择 CRC 类型 / 算法，设置多项式、初值、异或等
- **字段排序**：定义好的字段可拖拽调整顺序
- **保存复用**：帧定义保存为"帧模板"可复用，支持导入 / 导出（JSON）
- **多帧管理**：一帧对应一个帧定义，切换 TAB 管理多帧
- **导入**：支持上传已有帧定义文件 / 抓包文件导入，两种方式（手动拖拽 + 导入）并存

### 3.3 解析层联动
- 自动校验（用定义的校验规则验 CRC / 和校验）
- 自动分帧 / 打标
- 条件触发（到某一帧 / 某个值自动响应）

### 3.4 发送能力
- 自定义指令帧 + 重复 / 定时发送
- 带字段填充的帧（协议字段填充后发送）
- 脚本化条件发送

#### 3.4.1 发送面板（交互形态）
- **指令队列主体**：多条指令列表，每条可编辑，支持上移 / 下移 / 删除 / 启用 / 禁用
- **指令内容格式**：支持 Hex 串和文本两种格式
- **发送模式（三种都要）**：
  - 顺序发送（一遍，可设每帧间隔）
  - 循环发送（无限循环）
  - 触发式（条件触发）
- **定时发送（三种）**：固定周期循环发一条 / 多条 + 手动点发（一次性或循环）+ 条件触发
- **条件触发（可配置）**：收到特定帧 / 匹配某字段值 → 触发；数据满足某条件（如速率、校验失败）→ 触发；时间到点 → 触发
- **指令与帧模板关系（两者都支持）**：指令可绑定帧模板直接发送一段完整帧，也可是自由 Hex / 文本内容
- **动态 / 反馈（全要）**：
  - 发送带时间戳 / 计数字段（如帧序号）动态填充
  - 发送前二次确认 / 预览
  - 发送成功 / 失败反馈（如解析对错）
- **保存预设**：指令模板保存为预设，存模板库（支持分组 / 重命名），随时调用
- **日志记录**：发送 / 接收统一日志视图，收发都可回放

### 3.5 通信连接与端口配置（上层配置区）
- **选端口**：下拉框自动扫描可用 COM 口
- **波特率**：常见速率下拉（9600 / 115200 / 38400 / 57600 等），可自定义输入
- **完整参数**：数据位（5-8）、停止位（1 / 2）、校验（无 / 奇 / 偶 / 标记 / 空）、流控（无 / RTS-CTS / XON-XOFF）
- **连接开关**：打开 / 关闭端口按钮，连接状态实时显示（断开 / 连接中 / 已连接 / 错误）
- **设备拔插**：检测到端口变化自动刷新 / 提示
- **配置记忆**：记住上次的端口 / 波特率等配置，下次自动恢复
- **收发计数**：打开端口时实时显示收发字节计数
- **连接锁定**：连接正常时禁用参数修改，防止误改
- **端口信息**：显示端口描述 / 设备名 / 硬件 ID

---

## 4. 技术架构

### 4.1 技术栈
| 层 | 技术 | 理由 |
|----|------|------|
| UI 前端 | **Flutter**（Google 开源套件，Material 风格） | 组件库丰富可复用，主打观赏性 / 动效，Win 桌面为主 |
| 后端 | **Rust** | 串口 + 协议分析，**性能优先**——调试工具绝不能卡顿 |
| 插件 | 双轨：普通插件 **Python** / 高性能插件 **Rust** | 普通功能 Python 易写，高性能用 Rust |

### 4.2 架构原则
- **单一主程序（Single App）+ 能力插件化**
- **高性能后端与 UI 解耦**
- **Core 稳定优先，插件可失败**
- **初版就规划好标准插件接口**，考虑外部贡献者易用性（开源路线）

### 4.3 平台
- Windows 桌面端为主（主力开发环境）

---

## 5. 交互与体验

### 5.1 主界面布局
- **单窗口，三区主干 + 左右可折叠侧边栏**
- 左侧边栏（可折叠）：协议类型切换（UART / CAN / CAN-FD / IIC / TCP 等）+ 设置按钮
- 中间主干（上中下三区）：
  - **上层**：端口 / 波特率等连接配置
  - **中层**：接收区 —— 在标题栏切换 HEX 原始 / 文本原始，显示同一 RX/TX 数据流
  - **下层**：发送面板 —— **多条指令队列，顺序发送 / 定时循环**
- 右侧边栏（可折叠）：字段解析与主界面之外的特殊解析、其他扩展 / 插件功能入口

### 5.2 数据流视图
- 核心三件套布局：**自定义帧定义 + 字段填充发送面板 + 分层日志视图**
- HEX / 文本原始视图位于接收区标题栏切换；字段结构化解析位于右侧 Dock，二者共用同一数据源
- 结构化字段视图：**每帧一条可展开，可拖宽列、行列冻结，类表格形态**
- 显示样式：每条日志带**彩色方向标识（RX 蓝 / TX 绿）**，按帧分块高亮

### 5.3 应用级操作
- **工具栏**：打开 / 保存日志、导入 / 导出帧配置、清除日志等；实时保存从下拉菜单明确选择关闭 / CSV / TXT，编译默认与启动默认均为关闭，可在设置中启用续写上次选择的文件
- 发送面板：多条指令队列，顺序发送 / 定时循环，支持保存常用指令 / 预设

### 5.4 设计语言
- **设计基准：Google Material 3（Material You）** —— 详见 `设计规范.md`
- **字体**：Roboto（界面）+ Roboto Mono（数据/HEX）
- **图标**：Material Symbols Rounded（Google 官方图标）
- **组件**：M3 原版（Navigation Rail / Filled Button / Segmented Button / Primary Tabs / Outlined Field / Snackbar）
- 调性：结构清晰、专业、现代
- 主题：**深色为主，支持一键切换浅色**
- **视觉原型**：`sketches/002-material3/index.html`（已确认采纳，可直接在浏览器打开查看）
- 选型理由：Flutter 内置 Material 组件即 M3，原型可 1:1 落地，开发成本最低

### 5.5 界面语言
- **中英双语，中文为主**

---

## 6. 非目标（明确不做）

- **不做示波器 / 逻辑分析仪 / 高速波形抓取**（重分析类，不来）
- 第一版 **不做** TCP / 蓝牙 / UDP 等扩展通信
- **不做大而全**，核心保持轻量
- 不做安全 / 加密 / 云端远控（当前阶段）

> 注：旁路监听（Virtual COM Pair 软件夹子）为**远期高级能力**，当前不开发，仅留作后续插件演进方向。

---

## 7. 开发阶段规划（Milestones）

> 「写速快」与「不能一卡一卡」是贯穿全程的两条底线。

### Phase 0：冻结需求与边界
- [x] 明确 Core / Plugin 边界
- [x] 本产品说明书建立

### Phase 1：框架基座 + 设计系统
- [x] HCOM 主应用框架（Flutter 壳 + Rust 后端通信协议）
- [x] Material 风格设计语言 / 动效 / 主题确定
- [x] 单窗口多面板（Dock / Tab）布局骨架

### Phase 2：UART 通信内核
- [x] Rust 后端串口读写（Windows COM）
- [x] 双向收发（RX / TX）
- [x] 连接 / 断开状态机

### Phase 3：协议帧解析
- [x] 可视化帧定义（拖拽配置）
- [x] 帧头 / 帧尾 / 长度 / 大小端 / 校验
- [x] 解析结果分层展示（Hex / 字段 / 时间戳方向）

### Phase 4：发送面板
- [ ] 自定义指令帧 + 重复 / 定时发送
- [ ] 字段填充发送
- [ ] 脚本化条件发送（循序渐进）

### Phase 5：插件系统 v0
- [ ] 插件目录扫描 / manifest 解析
- [ ] 初版标准插件接口
- [ ] 双轨插件（Python / Rust）示例

### Phase 6：联动与自动分帧
- [ ] 校验联动 / 自动分帧打标
- [ ] 条件触发

---

## 8. 成功判定标准（Definition of Done）

- Core 单独使用即可完成日常 UART 调试（收发 + 解析 + 发送）
- 插件可独立安装与卸载
- 工具**连续运行 ≥ 8 小时无崩溃**
- 数据流处理**不卡顿**（性能优先）

---

## 9. 演进记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-09 | 会谈定稿：推翻旧"旁路监听优先"定义，确立"UART 通信 + 协议帧解析"为核心 |
